import { extendTheme } from '@chakra-ui/react';

const customTheme = extendTheme({
  fonts: {
    body: 'YourFont, sans-serif', // 例: 'Arial, sans-serif'
    heading: 'YourFont, sans-serif', // 例: 'Arial, sans-serif'
  },
});

export default customTheme;


